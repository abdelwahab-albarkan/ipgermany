# دليل قواعد وهندسة البرمجة (CODING_RULES.md)
## المبادئ البرمجية الصارمة ومعايير كتابة الأكواد لمشروع STREAMB4 باللغة العربية

يلتزم جميع مطوري الفريق باتباع هذه القواعد البرمجية لضمان كود برمجي نظيف وقابل للصيانة وخالٍ من الأخطاء التراكمية.

---

### 1. قواعد ومبادئ React 19
*   **خادم الخادم أولاً (Server Components by default):** يتم بناء جميع المكونات والصفحات كمكونات خادم (RSC). يمنع استخدام `'use client'` إلا عند الضرورة القصوى للمكونات التفاعلية الدقيقة التي تحتاج لخطافات التفاعل (`useState`, `useEffect`) أو الحركات.
*   **تقليص إدارة الحالات (State Management):** بما أن المشروع يعتمد على توليد الصفحات الثابتة (Static Export)، يجب تفادي بناء مخازن حالات معقدة مثل (Redux) والاعتماد بدلاً من ذلك على الحالات المحلية لمكونات العميل، أو استخدام معلمات البحث المباشرة في الروابط (`URL Search Params`) لإدارة الفلاتر والبحث.
*   **منع الآثار الجانبية (Side Effects):** يفضل دائماً تجنب خطاف `useEffect` لصالح معالجات الأحداث المباشرة (Event Handlers) لمنع حدوث التكرار غير المرغوب فيه في دورة حياة المكونات.

---

### 2. معايير Next.js 15+ وهندسة المسارات
*   **مجلد `app/` (App Router):** الهيكل المعتمد هو مجلد المسارات المباشر. يتم تنظيم الصفحات التسويقية داخل مجلد افتراضي `(marketing)` لتوحيد شريط التنقل والتذييل، بينما تنفصل المدونة والصفحات القانونية بتخطيطاتها الخاصة.
*   **جلب البيانات الثابتة (Static Data Fetching):** يتم جلب كافة البيانات المطلوبة للمدونة أو القنوات من ملفات MDX أو ملفات JSON المحلية مباشرة أثناء وقت البناء (Build Time).
*   **تصدير التوليد الاستاتيكي الكامل:** إعداد تصدير المشروع مهيأ بالكامل لـ `output: 'export'` مع التأكد من عدم استخدام ميزات خادم نشطة أثناء تشغيل الواجهات للعميل.

---

### 3. اتفاقيات التسمية الموحدة (Naming Conventions)
*   **الملفات والمجلدات:**
    *   مجلدات المسارات والمكونات الفردية: نمط الكباب `kebab-case` (مثال: `pricing-grid.tsx` ، `use-debounce.ts`).
*   **المكونات البرمجية:** نمط الباسكال `PascalCase` (مثال: `PricingCard` ، `Navbar`).
*   **المتغيرات والوظائف:** نمط الجمل `camelCase` (مثال: `const isPopularPlan = true;` ، `function getPostBySlug()`).
*   **الثوابت والأنواع:** استخدام الأحرف الكبيرة للفصل بين الكلمات المترابطة بالشرطة السفلية للثوابت العامة (مثال: `export const BASE_URL = '...';`).

---

### 4. ترتيب استيراد الحزم والمكتبات (Imports Order)
يجب فرز الاستيرادات في كل ملف برمجياً وتلقائياً باتباع الترتيب التالي:
1.  المكتبات والوظائف الأساسية لـ React و Next.js.
2.  المكتبات الخارجية المساعدة (مثل `framer-motion`, `lucide-react`).
3.  مسارات الاستيراد المختصرة للموقع عبر العلامة `@/` (مثال: `@/components/ui`, `@/lib/utils`).
4.  الاستيرادات النسبية المباشرة (مثل `./component-style.css`).

---

### 5. قواعد الأداء واستخدام الذاكرة (Performance Rules)
*   **منع إعادة التوليد غير الضروري (Re-renders):** توظيف خطافات `useMemo` و `useCallback` فقط عند تمرير وظائف ثقيلة أو مصفوفات معقدة للمكونات الفرعية التفاعلية التي تم تحسينها بـ `memo`.
*   **التحميل الكسول للمكونات التفاعلية:** استيراد المكونات التفاعلية الكبيرة التي لا تقع في واجهة العميل الفورية (مثل جداول القنوات الطويلة أو منزلقات التقييمات) باستخدام `next/dynamic` لتقليص حجم حزمة الصفحة الأولى.

---

### 6. معايير TypeScript الصارمة
*   يمنع تماماً استخدام النوع غير المحدد `any`. يجب كتابة واجهات الأنواع والتعريفات (Interfaces & Types) لكافة الخصائص المستلمة والمسترجعة.
*   تفعيل ميزات التحقق الصارم في ملف `tsconfig.json` مثل `strictNullChecks` و `noImplicitAny`.

---

### 7. قواعد اختبار جودة الكود والتحقق (Testing Rules)
*   استخدام أداة اختبار التصفح الحقيقي (Playwright) للتحقق من سلامة روابط الشراء والنماذج التفاعلية قبل النشر.
*   تطبيق التحقق من التنسيقات باستخدام الأدوات البرمجية (ESLint & Prettier) قبل إرسال التحديثات لفرع الإنتاج الرئيسي.
