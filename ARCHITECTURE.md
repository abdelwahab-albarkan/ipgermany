# دليل البنية المعمارية وهندسة النظام (ARCHITECTURE.md)
## المخططات الهيكلية وإدارة تدفق البيانات لمشروع STREAMB4 باللغة العربية

يوضح هذا الدليل الهندسة البنيوية وهيكل الاتصال وتوزيع العمليات لضمان أداء فائق وتوافق تام مع البناء الاستاتيكي.

---

### 1. المخطط المعماري للنظام (High-Level Architecture Diagram)
يعتمد المشروع على بنية خالية من الخادم المباشر وقواعد البيانات النشطة (Jamstack Architecture) لتحقيق أقصى درجات الأمان والسرعة:

```
                  [ متصفح المستخدم العربي ]
                             │
                             ▼  (طلب الصفحة)
      [ شبكة توزيع محتوى Vercel Edge Serverless ]
                             │
         ┌───────────────────┴───────────────────┐
         ▼ (أصول ثابتة)                         ▼ (صفحات ديناميكية دورية)
   [ Vercel CDN ]                         [ Next.js 15+ Static Export ]
   - ملفات الخطوط المدمجة                  - مقالات المدونة (MDX)
   - الصور المهيأة والتصميم                 - الأسعار والأسئلة الشائعة
   - أكواد التنسيق CSS / JS                - قائمة القنوات (JSON محلي)
```

---

### 2. تدفق البيانات (Data Flow)
بما أن المشروع لا يحتوي على قاعدة بيانات خلفية، يتم التعامل مع تدفق البيانات على النحو التالي:
1.  **بيانات المدونة وأدلة التشغيل:** يتم قراءتها ومعالجتها مباشرة من ملفات MDX المحلية باستخدام معالج المحتوى المكتوب في `lib/mdx.ts` أثناء مرحلة البناء والتجميع.
2.  **بيانات القنوات والأجهزة المتاحة:** يتم تخزينها كملفات JSON ثابتة ومحدثة داخل المشروع، ويتم فلترتها بالكامل في متصفح الزائر باستخدام وظائف البحث التفاعلية المكتوبة بلغة TypeScript لضمان سرعة رد فعل فورية خالية من زمن استجابة الخادم.
3.  **إعادة التوليد الدوري (ISR):** تتم عملية إعادة بناء وتحديث للموقع تلقائياً كل 24 ساعة للتحقق من أي تغييرات في ملفات المحتوى على GitHub وإتاحتها للزوار.

---

### 3. استراتيجية إدارة الحالات (State Management Strategy)
لتجنب استهلاك موارد المتصفح والمحافظة على سرعة أداء مثالية:
*   **الحالات المحلية (React State):** نستخدم خطافات `useState` فقط للمكونات التفاعلية الصغيرة والمعزولة (مثل تمدد لوحة الأسئلة الشائعة، أو فتح القائمة المنسدلة).
*   **إدارة حالات البحث عن طريق الروابط (URL-state):** يتم الاعتماد على معلمات روابط البحث في المتصفح (`?search=osn&category=sports`) للتحكم في نتائج تصفية القنوات، مما يتيح للمستخدم مشاركة روابط الفلترة المباشرة مع زملائه وتعزيز تهيئة محركات البحث (SEO).

---

### 4. نقاط التكامل والروابط الخارجية (API Integration Points)
*   **نموذج الاتصال والدعم الفني:** يتم ربط نموذج اتصل بنا بخدمة خارجية لمعالجة الرسائل (مثل Formspree أو Webhook مخصص) مباشرة عن طريق بروتوكول HTTP POST آمن دون المرور بخوادم خاصة بالمشروع.
*   **بوابات الشراء والتحويل المالي:** يتم توجيه المشترك مباشرة من أزرار الباقات التسويقية إلى روابط بوابات الدفع الخارجية المؤمنة (مثل Stripe Checkout أو بوابات الدفع المحلية) لإكمال عملية الشراء بأمان تام.

---

### 5. استراتيجية النشر والتحكم بالمتغيرات (Deployment & Env Variables)
*   **استراتيجية النشر:** يتم النشر بشكل تلقائي وفوري على منصة Vercel بمجرد دمج التعديلات أو دفع الكود لفرع الإنتاج الرئيسي على GitHub.
*   **متغيرات البيئة المطلوبة (Environment Variables):**
    *   `NEXT_PUBLIC_SITE_URL`: الرابط الأساسي الفعلي للموقع (تستخدم لتوليد روابط خريطة الموقع والروابط الأساسية).
    *   `NEXT_PUBLIC_FORM_ENDPOINT`: الرابط المخصص لاستلام رسائل نماذج الاتصال والدعم.
