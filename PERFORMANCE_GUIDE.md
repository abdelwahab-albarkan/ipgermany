# دليل تحسين الأداء وسرعة التحميل (PERFORMANCE_GUIDE.md)
## الاستراتيجية الشاملة لتحقيق سرعة خارقة ومؤشرات أداء 100% لموقع STREAMB4 باللغة العربية

يهدف هذا الدليل لتوضيح الشروط الصارمة لبناء وتصميم صفحات موقع STREAMB4 لضمان أداء فائق وتجربة مستخدم مثالية وخالية من البطء.

---

### 1. مؤشرات الأداء الحيوية المستهدفة (Core Web Vitals Targets)
يلتزم المشروع بتحقيق الأرقام والمؤشرات التالية على أداة Lighthouse لقياس الأداء للنسخة النهائية:
*   **درجة الأداء الإجمالية (Lighthouse Performance Score):** `>= 95`.
*   **سرعة تحميل الجزء الأكبر من المحتوى المرئي (LCP):** `<= 1.8 ثانية`.
*   **وقت التفاعل الأول للموقع (INP):** `<= 100 مللي ثانية`.
*   **التحول التخطيطي المتراكم للعناصر (CLS):** `<= 0.02` (يمنع حدوث أي قفزة أو حركة غير مرغوبة للواجهة أثناء التحميل).
*   **إجمالي وقت الحظر لجافا سكريبت (TBT):** `<= 150 مللي ثانية`.

---

### 2. استراتيجية تحسين وعرض الصور (Image Optimization Strategy)
*   **تنسيق الصور المعياري:** نعتمد كلياً على صيغ الجيل الجديد الأكثر ضغطاً ومحافظة على الجودة (WebP و AVIF). يمنع استخدام صيغ PNG أو JPG للمشاهد الترويجية إلا في الحالات الاستثنائية.
*   **أبعاد ثابتة لمنع CLS:** يجب تحديد عرض وطول دقيقين لكل صورة (`width` و `height`) أو استخدام وسم الاحتواء التلقائي لضمان حجز المتصفح للمساحة قبل تحميل الصورة.
*   **تحميل أولوية الصور المباشرة (Priority Images):** يجب تفعيل خاصية `priority={true}` للصور الترويجية وأيقونات الهوية البصرية التي تقع في الجزء الأول من الشاشة (Above the fold) لتسريع وصول وقياس LCP.
*   **التحميل الكسول الافتراضي (Lazy Loading):** تعيين خاصية التحميل البطيء لجميع الصور الواقعة أسفل خط الطي بشكل تلقائي لمنع استهلاك بيانات الزائر.

---

### 3. استراتيجية تحميل الخطوط محلياً (Font Loading Strategy)
*   يتم استضافة ملفات الخطوط العربية الفاخرة (`Cairo-Variable.woff2` و `Tajawal-Variable.woff2`) محلياً داخل مجلد `public/fonts/` بالموقع. يمنع جلب الخطوط عبر خدمات Google Fonts المباشرة لتجنب فترات حظر المتصفح لترجمة الخط.
*   استخدام خاصية `display: swap` في تهيئة الخطوط لتجبر المتصفح على إظهار نص النظام الافتراضي فوراً ريثما يكتمل تحميل الخط الفاخر دون تجميد الصفحة.

---

### 4. استراتيجية تقسيم الأكواد وتقليص جافا سكريبت (Code Splitting Strategy)
*   يتم تجزئة المكونات التفاعلية والصفحات المعقدة باستخدام الاستيراد الديناميكي `next/dynamic` للتأكد من عدم تحميل المكون إلا عند تفاعل المستخدم الفعلي معه.
*   يجب إبقاء المكونات التي تتطلب خطافات عميل في أضيق نطاق لعزل الحزمة التفاعلية وتقليل تكاليف الترطيب (Hydration Cost) لدى متصفح الزائر.

---

### 5. ميزانية أحجام الحزم البرمجية (Bundle Size Budget)
نفرض قيوداً صارمة على أحجام ملفات جافا سكريبت المرسلة للمتصفح:
*   حجم حزمة جافا سكريبت الأساسية للصفحة الأولى (First Load JS): `<= 85 كيلوبايت`.
*   مجموع حجم ملفات الأنماط والتنسيق (CSS Bundle Size): `<= 20 كيلوبايت`.
*   حزم المكونات التفاعلية المستوردة ديناميكياً: `<= 15 كيلوبايت` لكل حزمة تفاعلية منفصلة.

---

### 6. قواعد التخزين المؤقت وتوزيع الشبكة (Caching Strategy)
*   يتم استضافة كامل أصول ومقالات ومكونات الموقع وتخزينها سحابياً على خوادم Edge لشركة Vercel.
*   يتم تفعيل خاصية إعادة التوليد الدوري (ISR) بمعدل مرة كل 24 ساعة للمحافظة على تحديث القنوات والأسعار دون الحاجة لإعادة بناء الموقع بالكامل يدوياً.
*   تحديد فترات التخزين المؤقت الطويلة للأصول الثابتة (مثل الشعار والخطوط) في المتصفحات للزوار المتكررين لرفع سرعة الاستجابة المباشرة.
